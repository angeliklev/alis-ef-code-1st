@model IEnumerable<AlisEF102.Models.AssignedTo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            AssignedDate
        </th>
        <th>
            ReturnDate
        </th>
        <th>
            Borrower
        </th>
        <th>
            Asset
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AssignedDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ReturnDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Borrower.BarCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Asset.BarCode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AssignedToID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AssignedToID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AssignedToID })
        </td>
    </tr>
}

</table>
