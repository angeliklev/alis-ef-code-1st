@model IEnumerable<AlisEF102.Models.AssetCondition>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            Description
        </th>
        <th>
            IssuedDate
        </th>
        <th>
            Asset
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IssuedDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Asset.BarCode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AssetConditionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AssetConditionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AssetConditionID })
        </td>
    </tr>
}

</table>
