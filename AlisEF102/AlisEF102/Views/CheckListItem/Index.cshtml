@model IEnumerable<AlisEF102.Models.CheckListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            CheckListItemName
        </th>
        <th>
            Asset
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CheckListItemName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Asset.BarCode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CheckListItemID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CheckListItemID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CheckListItemID })
        </td>
    </tr>
}

</table>
