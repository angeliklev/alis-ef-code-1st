@model AlisEF102.Models.CheckListItem

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>CheckListItem</legend>

        @Html.HiddenFor(model => model.CheckListItemID)

        <div class="editor-label">
            @Html.LabelFor(model => model.CheckListItemName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.CheckListItemName)
            @Html.ValidationMessageFor(model => model.CheckListItemName)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
