@model IEnumerable<AlisFirst.Models.AssignedTo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th></th>
        <th>
            AssignedDate
        </th>
        <th>
            ReturnDate
        </th>
        <th>
            Asset
        </th>
        <th>
            Borrower
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AssignedToID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AssignedToID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AssignedToID })
        </td>
        <td>
			@String.Format("{0:g}", item.AssignedDate)
        </td>
        <td>
			@String.Format("{0:g}", item.ReturnDate)
        </td>
        <td>
			@Html.DisplayTextFor(_ => item.Asset).ToString()
        </td>
        <td>
			@(item.Borrower == null ? "None" : item.Borrower.Surname)
        </td>
    </tr>
}

</table>


