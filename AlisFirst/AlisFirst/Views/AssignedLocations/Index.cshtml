@model IEnumerable<AlisFirst.Models.AssignedLocation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th></th>
        <th>
            AssignedLocationDate
        </th>
        <th>
            Asset
        </th>
        <th>
            Location
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AssignedLocationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AssignedLocationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AssignedLocationID })
        </td>
        <td>
			@String.Format("{0:g}", item.AssignedLocationDate)
        </td>
        <td>
			@Html.DisplayTextFor(_ => item.Asset).ToString()
        </td>
        <td>
			@Html.DisplayTextFor(_ => item.Location).ToString()
        </td>
    </tr>
}

</table>


