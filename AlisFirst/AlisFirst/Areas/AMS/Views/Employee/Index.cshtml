@model IEnumerable<AlisFirst.ViewModels.ListEmployeeViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            Borrower ID
        </th>
        <th>
            Bar Code
        </th>
        <th>
            Surname
        </th>
        <th>
            Given Name
        </th>
        <th>
            Borrower Expiry Date
        </th>
        <th>
            Email
        </th>
        <th>
            Phone Number
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BorrowerID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BarCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Surname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GivenName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BorrowerExpiryDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PhoneNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BorrowerID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BorrowerID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BorrowerID })
        </td>
    </tr>
}

</table>
